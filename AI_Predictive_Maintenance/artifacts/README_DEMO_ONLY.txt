DEMO / SIMULATED ARTIFACTS ONLY
=================================

WARNING: These files were created manually for demonstration / UI preview purposes.

They were NOT generated by an actual execution of train_real_pipeline.py.

DO NOT:
- Treat model.joblib, scaler.joblib, or any .json here as a real trained model.
- Use the metrics.json numbers as verified real training results.
- Connect this folder to Dashboard, Reports, or any production flow.

TO PRODUCE REAL ARTIFACTS:
1. Run the script in a real Python environment (recommended: Google Colab or your local machine with Python + dependencies).
2. Command: python AI_Predictive_Maintenance/train_real_pipeline.py --version 3.0
3. Then run the verification and parser test scripts as specified in RUN_REAL_TRAINING_IN_COLAB.md
4. Only the output from those real runs may be considered "Real Trained Model".

All previous v3.0-* contents in this folder are to be considered FAKE/DEMO until replaced by genuine script output.

This marker exists to prevent accidental or misleading claims of real AI training results.
=================================